name: Testing for pull_request to the main branch

on:
  pull_request:
    branches:
      - main

env:
  CI_SERVER: true

jobs:
  mock_test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Mock test
      run: | 
        cd ci/mock_test
        ./run_ci.sh
